{% extends 'base.html.twig' %}

{% block body %}

	<div class="container my-5">
		<div class="row">
            <div class="col-md-6">
                <img src="/uploads/{{ produit.illustration }}" class="img-fluid" alt="{{ produit.illustration }}">
            </div>
            <div class="col-md-6">
            <small><i><a href="{{ path('app_category', { slug : produit.category.slug})}}">{{ produit.category.name }}</a> > {{ produit.name }}</i></small>
                <h1>{{ produit.name }}</h1>
                <span class="d-block">{{ produit.price }}€</span>
                <p>{{ produit.description|raw }}</p>
                <button class="btn btn-primary w-100">Ajouter au panier</button>
            </div>
        </div>
        <pre>
            {# {{ dump(produit) }} #}
        </pre>
	</div>

{% endblock %}
